<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Tagok névsora</title>
</head>

<body>
    <h1>Tagjaink</h1>
    <table class="table table-hover">
        <thead>
            <tr>
                <th>nr</th>
                <th>név</th>
                <th>kor</th>
                <th>város</th>
                <th>faj</th>
            </tr>
        </thead>
        <tbody>
            <% for (let k of members){ %>
                <tr class=
                <%if(k.species=="human"){%>
                    "table-primary"
                    <% } %>
                <%if(k.species=="alien"){%>
                    "table-secondary"
                    <% } %>
                <%if(k.species=="kiborg"){%>
                    "table-success"
                    <% } %>
                <%if(k.species=="intruder"){%>
                    "table-danger"
                    <% } %>
                <%if(k.species=="reptilian"){%>
                    "table-warning"
                    <% } %>
                
                
                >
                    <td><%=k.id%></td>
                    <td><%=k.name%></td>
                    <td><%=k.age%></td>
                    <td><%=k.city%></td>
                    <td><%=k.species%></td>
                </tr>
                <% } %>
        </tbody>
    </table>

</body>

</html>